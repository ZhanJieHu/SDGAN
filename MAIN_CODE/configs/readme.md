0. config files are used for setting configations in model.
1. config files will be expanded after being read. the files in the floder are unexpanded version.
2. config_baby.yaml is filled with every item with comments.